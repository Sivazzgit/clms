# Apache ports configuration for ContractLabour / CLMS v2
# Managed via Dockerfile — do not edit at runtime.

Listen 80
Listen 5000
Listen 8083

<IfModule ssl_module>
    Listen 443
</IfModule>
<IfModule mod_gnutls.c>
    Listen 443
</IfModule>
